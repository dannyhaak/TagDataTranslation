name: Release iOS (CocoaPods + SPM)

on:
  push:
    tags: ["v*"]

jobs:
  build-xcframework:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Install iOS workload
        run: dotnet workload install maui-ios

      - name: Build XCFramework
        run: ./scripts/build-xcframework.sh

      - name: Create release zip
        run: |
          cd artifacts/ios
          zip -r TagDataTranslation.xcframework.zip TagDataTranslation.xcframework

      - name: Upload XCFramework to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/ios/TagDataTranslation.xcframework.zip

      # TODO: Add CocoaPods trunk push
      # - name: Push to CocoaPods
      #   run: pod trunk push sdk/swift/TagDataTranslation.podspec
      #   env:
      #     COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
